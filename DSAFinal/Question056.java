package DSAFinal;
import java.util.*;
public class Question056 {
   /* class Solution {
        public ListNode middleNode(ListNode head) {
            ListNode slow = head;
            ListNode fast = head;

            // Traverse the list with slow moving one step and fast moving two steps
            while (fast != null && fast.next != null) {
                slow = slow.next;
                fast = fast.next.next;
            }

            // slow will be at the middle node when the loop ends
            return slow;
        }
    }
*/
}
